import styled from 'styled-components'

/**
 * Je créer un style pour styliser un gros titre
 * en rouge.
 */
export const Container = styled.div`
  display: flex;
  justify-content: center;
`

/**
 * Contient le titre du big text
 */
export const Title = styled.h1`
  color: red;
  text-align: center;
`
